apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubelet-stats-exporter
spec:
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      name: kubelet-stats-exporter
  template:
    metadata:
      labels:
        name: kubelet-stats-exporter
    spec:
      containers:
      - name: kubelet-stats-exporter
        image: python:3.9-slim # Change to your image
        # Dummy command and args for testing 
        command: ['/bin/sh']
        args: ['-c','while true; do echo hello; sleep 60;done']
        ports:
        - containerPort: 9118
        # Increase these resource values for larger clusters
        resources:
          limits:
            cpu: 50m
            memory: 150Mi
          requests:
            cpu: 10m
            memory: 50Mi
      serviceAccountName: kubelet-stats-exporter  